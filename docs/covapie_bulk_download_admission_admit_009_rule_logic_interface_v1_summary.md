# CovaPIE ADMIT_009 standalone evaluator interface v1

This step implements the pure in-memory public API `evaluate_admit_009(duplicate_identity_key, batch_duplicate_identity_keys, duplicate_identity_key_contract)` and the exact frozen `Admit009EvaluationResult` dataclass. The result has the committed Exact10 field order, rejects subclasses and non-exact field types, remains compatible with `vars(result)`, and enforces `passed` exactly when `outcome == "passed"` plus `blocks_candidate` exactly when `outcome != "passed"`.

The evaluator validates in strict short-circuit order: candidate scalar, policy contract, batch snapshot, then exact membership. Candidate keys must be exact built-in strings matching `^covapie_dup_v1_sha256_[0-9a-f]{64}$`; the canonical value is the unchanged input. The policy must be the exact built-in string `covapie_duplicate_identity_key_contract_v1`. The batch must be an exact built-in tuple whose exact-string members are individually canonical, strictly ascending, and unique. No coercion, trimming, case conversion, Unicode normalization, aliasing, repair, sorting, deduplication, key reconstruction, or digest recomputation occurs.

Scalar-invalid results clear canonical and validated state and consume no context. Policy-invalid results retain the canonical pair and consume only `duplicate_identity_key_contract`. Batch-invalid results retain the pair and consume both context items. A key absent from the batch passes; a key present in the batch is blocked with `DUPLICATE_IDENTITY_KEY_ALREADY_PRESENT`. Every invalid or blocked result blocks the candidate, and every non-passed result has a frozen non-empty reason.

The formal evaluator does not import or call `classify_admit_009_duplicate_identity_key_design`. The independent checker compares all ten formal fields against both its checker-owned expectation and the committed design oracle for the ordered Exact32 corpus. Exact32 passes 32/32 with groups scalar 12, policy 4, batch 12, and outcome/state 4. The natural contract evidence has 46 rows, the fixed source boundary has 13 committed inputs, and the safety audit has 30 rows.

The public evaluator closure is exactly `evaluate_admit_009`, `_validate_scalar`, `_validate_policy`, `_validate_batch`, and `_result`; it performs no filesystem, subprocess, git, environment, network, materialization, design-gate, provider, torch, numpy, or RDKit work. Metadata materialization is isolated in the latter half of the module and writes six deterministic regular non-symlink outputs.

The Exact11 issue inventory is byte-identical to the committed ADMIT_009 design inventory. `DUPLICATE_IDENTITY_KEY_SEMANTICS_UNRESOLVED` remains resolved with transition `duplicate_identity_key_contract_frozen_v1`. Unified coverage remains open for ADMIT_009 through ADMIT_015 and ADMIT_009 is not removed from that coverage scope.

Real provider duplicate-key mapping remains unvalidated and the real provider key count remains zero. No provider key generation, unified adapter design or implementation, ADMIT_009 registration, Exact9 runtime, ADMIT_010, all-rules API, combined verdict, raw read, network/download, checkpoint use, model forward/loss, training, fine-tuning, or parameter update was performed. Exact8 runtime is unchanged. Formal training still requires a separate feature-semantics audit; Step12D remains only a smoke-legality check. The next authorized step is `design_covapie_admit_009_unified_adapter_contract_v1`.
