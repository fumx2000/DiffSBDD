# CovaPIE unified dispatch runtime with ADMIT_001 to ADMIT_008 v1

This successor implements the frozen `ADMIT_008 / topology_restoration_disposition` adapter and extends the immutable single-rule registry from Exact7 to Exact8. The public `UnifiedAdmissionRuleEvaluation`, `UnifiedAdmissionDispatchError`, Exact13/Exact6 fields, schema and outcome constants are the Exact7 predecessor objects. The `evaluate_admission_rule` signature and global exact-string, known-rule, registered-rule, handler precedence are unchanged. ADMIT_001 through ADMIT_007 handlers are reused directly by object identity; only `_evaluate_registered_admit_008` is new.

ADMIT_008 routes contexts in batch, evaluation-Mapping, required evaluation key, download-result, and stage-authorization order before any candidate access. It reads `allowed_topology_restoration_dispositions` exactly once, retains the original object, ignores unrelated context keys, and performs no adapter-side context-value validation. Candidate mappings and subclasses are accepted without copying, mutation or unrelated-field iteration. A non-Mapping candidate produces the existing Exact13 invalid result; only absence of `topology_restoration_disposition` is adapter missing.

Exact `None`, exact empty strings, empty-string subclasses, whitespace, malformed objects, unknown values, and both blocked dispositions are forwarded unchanged. Each key-present candidate calls `evaluate_admit_008` exactly once positionally with the original scalar and context objects. The source must be the exact committed `Admit008EvaluationResult`; its exact built-in-dict storage order, dataclass field order, all Exact10 reads, committed-class reconstruction, equality and cross-field invariants are validated before the oracle can run.

After source prevalidation, `classify_admit_008_topology_restoration_disposition_design` is called exactly once with the same objects. Its `classification.admit_008` view is used to construct a complete committed Exact10 result. Source and expected result must be exact-type and fully equal before projection. Failures return adapter-not-ready with `adapter_ready=false`, never call later stages, and never create partial Exact13 state.

Projection uses `source.validated_candidate_fields` for both normalized and validated values. `approved_restoration_template` and `explicit_manual_review_approved` pass. `manual_review_required` and `quarantine_required` remain blocked with their distinct frozen reasons. Scalar-invalid results retain empty normalized/validated state; context-invalid canonical values retain the canonical pair.

The deterministic evidence contains 77 runtime-contract rows, 203 truth rows in nine natural groups, 15 registry rows, 41 safety rows and the successor Exact11 issue inventory. Truth includes all 107 committed Exact7 predecessor rows, seven predecessor-handler identity rows, the complete ADMIT_008 Exact38 corpus, ordered context and candidate cases, source/oracle fail-closed cases, projections and the unsupported boundary. The registry audit passes 15/15: ADMIT_001–007 are predecessor identities, ADMIT_008 is the sole successor handler, and ADMIT_009–015 remain known but unregistered.

The authoritative issue predecessor is the ADMIT_008 adapter-design inventory at SHA256 `229251600f0c6ae389633fff86af8859280b86664521ecce04c494906dc39695`. Only `UNIFIED_ADMISSION_RULE_COVERAGE_INCOMPLETE` changes: it remains open and blocking, now covers ADMIT_009 through ADMIT_015, and records `admit_008_implemented_and_removed_from_open_coverage_scope`. All other rows are unchanged, including the resolved topology-disposition and covalent-evidence enum issues and the open provider, duplicate, leakage, raw, download and aggregation blockers.

The metadata gate verifies the fixed ordered Exact18 committed-source boundary as tracked base-tree blobs, regular non-symlink filesystem files, safe descendants, and identical expected/base/filesystem SHA256 values. All structural checks precede the first explicit source-byte read, frozen snapshot bytes alone are parsed, and artifact-reference paths are not followed recursively. The independent checker freezes the complete manifest dictionary, readiness mirrors, every output row and all six output SHA256 values; it also rejects semantic tampering after a synchronized manifest-hash refresh.

No Exact7 file or ADMIT_001–007 handler was modified. No provider mapping, provider value materialization, restoration provenance consumption, real candidate evaluation, ADMIT_009 implementation or registration, all-rules API, combined verdict, precedence change, raw access, network/download, checkpoint use, scientific/model runtime, forward/loss, training, fine-tuning or parameter update occurred. Bulk download and training remain forbidden. A feature-semantics audit remains mandatory before training; Step12D was only a smoke-legality check and not the final training-feature contract. The only recommended next step is `audit_covapie_admit_009_formal_evaluator_interface_preconditions_v1`, which is not started here.
