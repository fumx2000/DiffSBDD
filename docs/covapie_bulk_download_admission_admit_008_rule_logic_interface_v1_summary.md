# CovaPIE ADMIT_008 standalone evaluator interface v1

This increment implements the pure in-memory standalone evaluator `evaluate_admit_008(topology_restoration_disposition, allowed_topology_restoration_dispositions)` and the frozen `Admit008EvaluationResult`. Both public inputs are required positional-or-keyword direct objects. The evaluator does not accept a candidate mapping, own candidate-key missing behavior, consume restoration-rule or provenance fields, perform provider mapping, or use I/O.

The result is a frozen exact-type dataclass with the ordered Exact10 fields `admission_rule_id`, `outcome`, `passed`, `blocks_candidate`, `reason`, `canonical_topology_restoration_disposition`, `validated_candidate_fields`, `consumed_candidate_fields`, `consumed_context_items`, and `evaluator_io_used`. Construction fails closed for subclasses, non-exact strings or booleans, non-exact tuple state, malformed pairs, vocabulary drift, inconsistent outcome flags or reasons, invalid canonical state, consumed-field drift, and any I/O claim.

The ordered Exact4 dispositions are `approved_restoration_template`, `explicit_manual_review_approved`, `manual_review_required`, and `quarantine_required`. The exact allowed context is the built-in tuple containing only the first two values in that order. Scalar precedence is exact built-in `str`, nonempty, ASCII, syntax `[a-z][a-z0-9_]{0,63}`, then Exact4 membership. Context precedence is exact built-in `tuple`, exact built-in `str` members, then exact content and order. No trimming, case conversion, aliasing, Unicode normalization, repair, rule-ID conversion, or catch-all is performed.

The first two canonical members pass with an empty reason. `manual_review_required` blocks with `TOPOLOGY_RESTORATION_MANUAL_REVIEW_REQUIRED`; `quarantine_required` blocks with `TOPOLOGY_RESTORATION_QUARANTINE_REQUIRED`. The two blocked reasons are distinct and cannot be exchanged. Scalar failure short-circuits context validation. Scalar-invalid results have empty canonical and validated state; canonical scalar results retain the exact single validated pair on context-invalid, passed, and blocked paths. The historical lowercase `topology_restoration_unapproved` is not in the standalone reason vocabulary.

The evaluator transitive closure is exactly `evaluate_admit_008`, `_validate_scalar`, `_validate_context`, and `_result`. AST checks attest that it is pure in memory and that production neither imports nor calls `classify_admit_008_topology_restoration_disposition_design`. The checker and tests independently call that committed design oracle with the same raw scalar and context objects, independently construct the expected Exact10 result, and compare every field against production.

The natural Exact38 synthetic corpus passes 38/38: canonical 4, scalar type 6, empty/syntax 11, unknown 5, and context 12. The deterministic contract contains 37 natural rows. The source audit contains the frozen ordered Exact11 boundary, with every path tracked, present as a base-tree blob, regular, non-symlinked, and equal to its expected SHA256 after all structural checks complete before the first source-byte read. Artifact references are not recursively followed.

The authoritative enum-design Exact11 issue inventory is preserved byte-for-byte at SHA256 `229251600f0c6ae389633fff86af8859280b86664521ecce04c494906dc39695`. `TOPOLOGY_DISPOSITION_ENUM_UNRESOLVED` remains resolved. Unified coverage remains open and still includes ADMIT_008 through ADMIT_015; standalone implementation alone does not perform the future runtime coverage transition.

The 25-row safety audit records standalone implementation, Exact10 validation, scalar/context validation, Exact38 truth, independent oracle equivalence, source verification, and evaluator purity as executed. Provider mapping and provider values, unified adapter work, Exact7 runtime modification, ADMIT_008 registration, ADMIT_009, `evaluate_all_rules`, combined verdict, real candidate evaluation, raw access, network or bulk download, checkpoints, scientific/model libraries, model forward or loss, fine-tuning, training, and parameter updates were not executed.

Readiness is true for the standalone evaluator/result/scalar/context/reason contracts, Exact38 and independent-oracle evidence, pure evaluator closure, provider-mapping boundary preservation, readiness for ADMIT_008 unified-adapter contract design, and the mandatory feature-semantics audit before training. Real provider mapping remains unvalidated with zero real provider values. Adapter design and implementation, runtime registration, ADMIT_009, all-rule coverage, aggregation, real evaluation, bulk download, and training readiness remain false.

The only recommended next step is `design_covapie_admit_008_unified_adapter_contract_v1`; this increment stops before that step. Before any formal training, fine-tuning, training preparation, or parameter update, a separate feature-semantics audit remains mandatory. Step12D was only a smoke-legality check, not a final training-feature contract, and the historical `UNKNOWN_ATOM_FEATURE_POLICY` / `feature_semantics_known=False` state remains a training blocker until resolved or formally audited.
