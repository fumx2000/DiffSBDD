# CovaPIE ADMIT_008 topology restoration disposition enum contract design gate v1

This metadata-only gate freezes the standalone direct-scalar contract for `ADMIT_008 / topology_restoration_disposition`. The future evaluator signature has two required positional-or-keyword inputs: `topology_restoration_disposition` and `allowed_topology_restoration_dispositions`. It does not accept a candidate mapping, own candidate-key missing behavior, or consume restoration-rule/provider provenance.

The ordered canonical Exact4 enum is `approved_restoration_template`, `explicit_manual_review_approved`, `manual_review_required`, and `quarantine_required`. The exact built-in tuple allowed context contains only the first two values, in that order. Scalars require exact built-in `str`, nonempty ASCII, syntax `[a-z][a-z0-9_]{0,63}`, and Exact4 membership, with no trimming, case folding, aliases, Unicode normalization, rule-ID conversion, repair, or catch-all. Context requires exact built-in `tuple`, exact built-in `str` members, and Exact2 content/order.

The first two enum members pass. `manual_review_required` blocks with `TOPOLOGY_RESTORATION_MANUAL_REVIEW_REQUIRED`; `quarantine_required` blocks with `TOPOLOGY_RESTORATION_QUARANTINE_REQUIRED`. Scalar failure wins over context failure. Invalid or unknown scalar state has empty canonical and validated state; canonical scalar with invalid context, passed state, and blocked state retain the canonical validated pair.

The independent pure design oracle is `classify_admit_008_topology_restoration_disposition_design`. It is not `evaluate_admit_008`, performs no I/O or provider access, and must not be called by the future production evaluator. The natural Exact38 truth corpus passes 38/38 across canonical 4, scalar-type 6, empty/syntax 11, unknown 5, and context 12 cases.

The Exact12 category mapping freezes fail-closed provenance semantics without executing provider mapping. Quarantine overrides template or manual-approval claims; unfinished review, deferred/new rules, and unvalidated templates cannot become approved; `manual_visual_review_required=true` is not completed approval; new residues and warheads cannot inherit approval; and V1 has no `not_applicable` or no-restoration bypass.

The frozen Exact15 source boundary is tracked, base-tree-backed, regular, non-symlinked, and SHA256-consistent. The Exact11 successor issue inventory changes only `TOPOLOGY_DISPOSITION_ENUM_UNRESOLVED`: it is resolved through `topology_restoration_disposition_enum_contract_frozen_v1`. Unified coverage remains open and continues to include ADMIT_008.

Therefore the standalone evaluator preconditions are complete and the only recommended next step is `implement_covapie_admit_008_standalone_evaluator_interface_v1`. Real provider mapping remains unvalidated with zero real provider values. `evaluate_admit_008`, `Admit008EvaluationResult`, adapter design/implementation, Exact7 runtime modification, ADMIT_008 registration, ADMIT_009, real candidate evaluation, bulk download, and training remain outside this step. Feature-semantics audit remains mandatory before training; Step12D was only a smoke-legality check.
