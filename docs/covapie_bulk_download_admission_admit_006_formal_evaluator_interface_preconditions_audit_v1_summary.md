# CovaPIE ADMIT_006 formal evaluator interface preconditions audit v1

This read-only semantic audit freezes an Exact12 committed source boundary and inventories 62 traceable field, rule, and blocker occurrences. The field `covalent_event_evidence_source` is a scalar on a future candidate record whose producer scope is `candidate_metadata_provider`; `allowed_covalent_evidence_classes` is a separate future-caller context item shared by ADMIT_006 and ADMIT_007. These roles are related, but they are not a frozen value enum.

The readable committed metadata contains zero concrete values for `covalent_event_evidence_source`. The historical phrase “explicit non-distance-only evidence source” is a value-contract description, not an enum member. No canonical enum, exact scalar type, empty/null handling, whitespace/case normalization, unknown-value handling, or value-level meaning for distance-only/manual-review markers is frozen.

ADMIT_006 has canonical required-status and blocking-reason names (`explicit_evidence_source_present`, `covalent_event_evidence_missing`), while ADMIT_007 has `not_distance_only_inference` and `distance_only_inference_not_admissible`. Both rules still depend on the same unresolved enum/context contract, so their value-level responsibility boundary is not sufficiently frozen. An exact nonempty-string presence check would incorrectly treat arbitrary strings as explicit covalent evidence and is therefore unsafe.

`COVALENT_EVIDENCE_ENUM_UNRESOLVED` remains open, blocking, and scoped to `ADMIT_006|ADMIT_007`. The current runtime remains Exact5 (`ADMIT_001`–`ADMIT_005`); ADMIT_006–015 remain unregistered, `evaluate_all_rules` remains absent, and the Exact11 provider, coverage, and cross-rule issue boundaries are preserved.

The recommended next step is `design_covapie_covalent_event_evidence_source_enum_contract_v1`. This audit does not implement or register ADMIT_006, modify ADMIT_007, execute a provider/parser, read raw structures or checkpoints, follow artifact references, materialize/evaluate real candidates or Exact11 rows, download data, or run model/training code. Bulk download and training remain forbidden. A feature-semantics audit remains mandatory before formal training; Step12D smoke legality must not be treated as a final training-feature contract, and the historical unknown-atom/`feature_semantics_known=False` state still requires resolution or formal audit.
