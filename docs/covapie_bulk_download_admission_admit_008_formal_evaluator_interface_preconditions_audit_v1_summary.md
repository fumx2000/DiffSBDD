# CovaPIE ADMIT_008 formal evaluator interface preconditions audit v1

This metadata-only, fail-closed audit confirms the identity `ADMIT_008 / topology_restoration_disposition`, the `pre_download` phase, and the candidate field `topology_restoration_disposition`. It does not implement a disposition enum, evaluator, result type, adapter, Exact8 runtime, provider mapping, candidate evaluation, download, or training path.

The Exact20 matrix passes its audit contract (20/20) while only 2 semantic areas are complete and 18 remain incomplete. In particular, no committed contract freezes a direct-scalar standalone projection, exact scalar type, null/empty/missing behavior, canonicalization or repair, ordered canonical enum, category mappings, restoration-rule/provenance coherence, `passed/blocked/invalid` mapping, uppercase reason vocabulary, canonical/validated state, runtime-context ownership, or an independent semantic oracle.

Step13M/Step13N provide authoritative high-level safety policy: the topology schema remains residue-agnostic; restoration rules are residue-warhead-specific; V1 is limited to current CYS/SG golden samples; the known CYS/SG acrylamide-like rule has manual-review provenance; unknown residue-warhead pairs are quarantined; new residues, warheads, and rules cannot be auto-generalized; and topology smoke cannot auto-restore ligand topology or claim training readiness. These policy strings, rule IDs, statuses, decisions, and booleans are not canonical ADMIT_008 scalar values.

The frozen Exact15 source boundary is tracked, base-tree-backed, regular, non-symlinked, and SHA256-consistent. The 22-row vocabulary inventory marks every observed string unsafe to promote. The 90-row occurrence inventory covers all required identity, field, policy, and blocker terms. No real provider `topology_restoration_disposition` scalar value exists in the committed metadata boundary (`real_provider_value_count=0`).

The Exact11 issue inventory is byte-identical to the current Exact7 runtime predecessor. `TOPOLOGY_DISPOSITION_ENUM_UNRESOLVED` remains `open` for `ADMIT_008`, and unified coverage still begins at `ADMIT_008`.

Therefore `ready_for_admit_008_standalone_evaluator_interface_implementation=false`, while `ready_for_admit_008_topology_restoration_disposition_enum_contract_design=true`. The only recommended next step is `design_covapie_admit_008_topology_restoration_disposition_enum_contract_v1`. Feature-semantics audit remains mandatory before training; Step12D was only a smoke-legality check.
