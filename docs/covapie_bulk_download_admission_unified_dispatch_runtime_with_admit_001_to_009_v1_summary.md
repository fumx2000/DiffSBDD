# CovaPIE unified dispatch runtime with ADMIT_001 to ADMIT_009 v1

This Exact9 successor implements the frozen `ADMIT_009 / duplicate_identity_precheck` unified adapter and extends the immutable single-rule registry from ADMIT_001–008 to ADMIT_001–009. Exact8 remains unchanged. `UnifiedAdmissionRuleEvaluation`, `UnifiedAdmissionDispatchError`, the result schema and fields, dispatch-error fields and codes, and outcome vocabulary are reused directly from Exact8 by object identity. The first eight registered handlers are also reused by object identity (8/8). The sole new handler is `_evaluate_registered_admit_009`.

The successor defines its own `evaluate_admission_rule` function object. Its signature is exactly equal to Exact8, while its global `EVALUATOR_REGISTRY` binding is the local Exact9 registry rather than the predecessor registry. Dispatch retains the exact built-in-string, known-rule, registered-rule, then local-handler precedence. ADMIT_010–015 remain known but unregistered, and the unchanged Exact8 dispatcher still reports ADMIT_009 as not registered.

ADMIT_009 validates all contexts before candidate access in the frozen order: batch Mapping, `batch_duplicate_identity_keys` direct lookup, evaluation Mapping, `duplicate_identity_key_contract` direct lookup, download-result None, then stage-authorization None. Mapping subclasses are accepted without copying, sizing, iteration, sorting, deduplication or normalization. Required keys are read exactly once by direct indexing; only `KeyError` means missing, while other Mapping exceptions propagate unchanged. Although validation routes batch before policy, unified results preserve the standalone canonical consumed-context order of policy then batch.

After all context gates, a non-Mapping candidate and an absent `duplicate_identity_key` produce adapter-generated invalid Exact13 results without formal or oracle calls. Every key-present scalar—including None, integers, empty strings, subclasses, whitespace, non-ASCII, malformed values and canonical keys—is forwarded unchanged. The standalone evaluator is called exactly once positionally with the original scalar, batch and policy objects.

The source must have the exact committed `Admit009EvaluationResult` type; subclasses are rejected. Validation covers exact built-in-dict storage, Exact10 storage order, committed dataclass field order, ordered reads of all ten fields, reconstruction with the committed class, complete reconstruction equality and all committed `__post_init__` invariants. Source failure reports adapter-not-ready with `adapter_ready=false`, prevents the oracle, and cannot emit a partial Exact13 result.

After source validation, `classify_admit_009_duplicate_identity_key_design` is called exactly once positionally with the same three original objects. Its Mapping classification is used to construct a complete committed Exact10 result; the rule ID is the frozen `ADMIT_009` constant and the other nine fields are obtained by exact required-key access. Only AttributeError, KeyError, TypeError and ValueError are converted to the frozen source-invariant failure. Source and oracle Exact10 objects must be fully equal before projection; partial matching is insufficient.

Exact10 projection preserves outcome, pass/block state, reason, validated fields, consumed fields, consumed contexts and evaluator I/O state. `source.validated_candidate_fields` supplies both normalized and validated values. The adapter correctly projects unique passed, duplicate blocked, scalar-invalid, policy-invalid and batch-invalid states, with `covapie_admit_009_unified_adapter_v1` as the adapter ID.

Deterministic evidence contains 79 contract rows, 289 truth rows in nine natural groups, 15 registry-audit rows, 43 safety rows and the successor Exact11 issue inventory. The truth matrix includes all 203 committed Exact8 truth rows, eight predecessor handler identity checks, the complete standalone Exact32 projection corpus, all six context errors and precedence, candidate and direct-lookup behavior, object identity and exactly-once calls, source/oracle failures, and registry/provider/aggregation stop boundaries. Registry audit passes 15/15: ADMIT_001–008 are predecessor identities, ADMIT_009 is the exact new handler, and ADMIT_010–015 remain unregistered.

The authoritative issue predecessor is the ADMIT_009 adapter-design inventory at SHA256 `2c461d4416742dda734789ba6768995c6d641fb2d1b6c460a0f80ea194482e92`. Only `UNIFIED_ADMISSION_RULE_COVERAGE_INCOMPLETE` changes: its affected rules become ADMIT_010–015 and its transition becomes `admit_009_implemented_and_removed_from_open_coverage_scope`. The other ten rows are unchanged, including resolved duplicate-key semantics and the open cross-rule aggregation issue.

Production and checker independently freeze the ordered Exact18 committed-source boundary and require tracked base-tree blobs, regular non-symlink filesystem files, safe descendants, and identical expected/base/filesystem SHA256 values. All structural checks precede the first explicit source-byte read. The adapter-design gate is source evidence only and is absent from the public runtime call graph. The independent checker freezes all output rows, the complete 146-key manifest dictionary, readiness mirrors and all six output SHA256 values; it also rejects semantic tampering after a synchronized manifest-hash refresh.

No provider mapping or real duplicate key was produced, and the real provider key count remains zero. ADMIT_010 was not started. No all-rules evaluator, combined candidate verdict or cross-rule precedence was implemented. Exact9 public runtime and Exact9 materializer did not read raw data, and neither read a checkpoint. During validation, however, a legacy real-provider checker was run in error: it read existing local raw data and temporarily rematerialized and modified eight tracked historical outputs. Those changes were restored exactly to HEAD; final tracked and staged residual diff counts are both zero. No network access or download occurred. This was a workflow instruction violation, now documented and contained, and it does not change Exact9 runtime semantics or the frozen output contract. Post-restoration validation uses an explicit safe checker allowlist, and the unsafe checker was not run again. No model path, forward/loss, backward pass, optimizer step, training, fine-tuning or parameter update occurred. Bulk download and training remain forbidden. Formal training still requires a separate feature-semantics audit; Step12D remains only a smoke-legality check, not a final training-feature contract. The only recommended next step is `audit_covapie_admit_010_formal_evaluator_interface_preconditions_v1`, which is not started here.
